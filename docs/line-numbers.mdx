# Code Block Line Numbers

## Default behavior (line numbers enabled)

```ts
function greet(name: string) {
  console.log(`Hello, ${name}`);
}
```

## Opt-out behavior (line numbers disabled)

```ts noLineNumbers
function farewell(name: string) {
  console.log(`Goodbye, ${name}`);
}
```
